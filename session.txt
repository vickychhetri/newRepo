SET SESSION
var store = sessions.NewCookieStore([]byte(secre-key))
session, _ := store.Get(r, "user-session")
session.Values["user_id] = 221
sessuion.Save(r,w)


GET FROM SESSION
session, _ =store.Get(r,"user-session")
session.Values["user_id"].(uInt)




Cookies
c : =http.Cookie{
    Name: "user_id",
    Value : "111",
    MaxAge : -1,
    path : "/",
    Httponly : true,
}
http.SetCookie(w, &c)


cookie, err:= r.Cookie("user_id)